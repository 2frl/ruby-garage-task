.header
  =@user.email
  =link_to 'Logout', destroy_user_session_path, method: :delete
.projects
  = render @projects
.addprojects
  = link_to create_project_path, {remote: true, class: "btn btn-project", :type => "button", method: :post} do
    .glyphicon.glyphicon-plus.project-plus
    Add TODO list
